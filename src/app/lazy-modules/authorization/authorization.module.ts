import { NgModule } from '@angular/core';
import { AuthorizationRoutingModule } from './authorization-routing.module';

@NgModule({
  imports: [AuthorizationRoutingModule],
})
export class AuthorizationModule {}
